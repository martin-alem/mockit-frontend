import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#021E39",
      contrastText: "#F6F9FC",
    },
    secondary: {
      main: "#626EE3",
      contrastText: "#F6F9FC",
    },
  },
});

export default theme;
